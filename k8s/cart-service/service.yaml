apiVersion: v1
kind: Service
metadata:
  name: cart-service
  namespace: message-queue
  labels:
    app: cart-service
spec:
  selector:
    app: cart-service
  type: NodePort
  ports:
    - name: http
      protocol: TCP
      port: 3001
      targetPort: 3001
      nodePort: 30001
