apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: message-queue
  labels:
    app: product-service
spec:
  selector:
    app: product-service
  type: NodePort
  ports:
    - name: http
      protocol: TCP
      port: 3003
      targetPort: 3003
      nodePort: 30003
