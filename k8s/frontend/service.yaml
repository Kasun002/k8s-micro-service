apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: message-queue
  labels:
    app: frontend
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
    - name: http
      protocol: TCP
      port: 3004
      targetPort: 3004
      nodePort: 30004
